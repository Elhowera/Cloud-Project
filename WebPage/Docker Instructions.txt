#Before anything go to the webpage folder
    cd WebPage

#First We build the WebPage Container
    docker build -t webpageapp -f WebServer.dockerfile .

#Second We build the Database Container
    docker build -t myxampp -f Database.dockerfile .

#Now we build the docker-compose file 
    docker-compose up -d   

#Then we go to and edit the database container and install nano and edit the config file to change the password to gain access
    docker exec -it webpage-myxampp-1 bash
    cd /opt/lampp/bin
    apt-get install nano
    nano /opt/lampp/phpmyadmin/config.inc.php
    change password to 4524 //$cfg['Servers'][$i]['password'] = '4524';